export const VALIDATION_SHEMA_TYPES = [
  'string',
  'number',
  'boolean',
  'date',
  'array',
  'object',
];

export enum ValidationShemaTypes {
  string = 'string',
  number = 'number',
  boolean = 'boolean',
  date = 'date',
  array = 'array',
  object = 'object',
}

export enum ValidationFieldTypes {
  required = 'required',
}
